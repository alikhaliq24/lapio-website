{
  "name": "lapio-website",
  "version": "1.0.0",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "npx serve public -p 3000",
    "build": "echo 'Static site — no build step required.'"
  }
}
